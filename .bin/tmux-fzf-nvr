#!/usr/bin/env bash
# fzf file picker → send to the session's nvim-server via nvr.
set -e
sel=$(fd --type f --hidden --exclude .git 2>/dev/null | fzf) || exit 0
[ -n "$sel" ] || exit 0
exec nvr "$PWD/$sel"
